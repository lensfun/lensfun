[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lensfun"
version= "${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_MICRO}"

[tool.setuptools]
packages = ["lensfun"]
